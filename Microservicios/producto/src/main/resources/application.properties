# Database
# spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/movie_now
# spring.datasource.username=${DB_USERNAME:movienow}
# spring.datasource.password=${DB_PASSWORD:Movie-now-123*}

# Database Configuration (MySQL) docker
spring.datasource.url=jdbc:mysql://mysql-db-products:3306/movie_now
spring.datasource.username=root
spring.datasource.password=rootpassword
spring.jpa.hibernate.ddl-auto=update

# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:schema.sql 
# spring.sql.init.data-locations=classpath:data.sql

# Genera schema
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Limpia la BD con cada ejecución
server.port=9070

#Config Eureka  
spring.application.name=productos
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.serviceUrl.defaultZone= http://eureka:8761/eureka
eureka.instance.hostname=producto
eureka.instance.instance-id=${spring.application.name}:${server.port}

